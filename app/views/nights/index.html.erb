<%#app/views/nights/index.html.erb%>
<br>
<br>
<br>
<div class="banner">
  <h1 class="font-beta"><span class="title-1">No</span><span class="title-2">Balls</span><span class="title-3">Just</span><span class="title-4">Bitches</span></h1>
    <h2 class="font-gamma">🎱🎱🎱 Here are your last nights 🎱🎱🎱</h2>
</div>

<%# ************** Display Places of Nights ************** %>
  <div class="container">
      <% @nights.each do |night| %>
      <div>
        <div class="list-name font-gamma"><%= night.place.name %></div>
        <div class="item-btn font-gamma"><%= link_to 'Show Night', night_path(night) %></div>
      </div>
      <% end %>
  </div>
    <div class="item-btn"><%= link_to 'Add new night', new_night_path(@night) %></div>
