<%#app/views/nights/index.html.erb%>
<h1 class="font-beta">Nights</h1>

<%# ************** TO DO ************** %>
<%# Create Nights Index Display %>
<%# Who will be able to add anything? %>
<%# Create Nights Footer? %>

<%# ************** Display Nights ************** %>
  <div class="container">
      <% @nights.each do |night| %>
      <div>
        <div class="card" style="width: 18rem;">
          <div class="card-body">
        <%# Create different nights %>
        <%# Who can create different nights? The members? Strangers?%>
        <%# <div class="col-xl-2 col-md-4 mb-4">
          <div class="card border-left-primary shadow h-100 py-2">
              <div class="row no-gutters align-items-center">
                <div class="col mr-2"></div> %>
                <h2 class="card-title mb-1 text-xs font-delta"> <%= night.title%></h2>
                <p class="mb-1 text-xs font-gamma"> <%= link_to 'Show Night', night_path(night)%></p>
                <p class="mb-1 text-xs font-gamma"> <%= link_to 'Add new night', new_night_path(@night)%></p>
            </div>
          </div>
              <%# </div>
          </div>
        </div>
      </div> %>
      <% end %>
  </div>
